{

    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run FastAPI",
            "type": "shell",
            "command": "python -m uvicorn app.api.main:app --reload",
            "group": "build",
            "problemMatcher": [],
            "detail": "Start FastAPI server with Uvicorn"
        }
    ]
}